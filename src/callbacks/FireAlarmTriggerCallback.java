package callbacks;

import data.persons.Person;

/**
 * Interface FireAlarmTriggerCallback
 * Handles callback for class FireAlarmTrigger
 */

public interface FireAlarmTriggerCallback {
    void callback(Person p);
}
